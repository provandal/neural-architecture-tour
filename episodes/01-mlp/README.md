# Episode 1 — MLP

**Status:** Deferred. Built last (after 2026-04-23).

The existing [Binary Digit Trainer](https://github.com/provandal/binarydigittrainer) covers this ground today and is linked from the series landing page. A port into this monorepo will happen after Erik's 2026-04-23 talk so the live URL in that presentation stays untouched.

## Planned triad

| Artifact | Planned content |
|---|---|
| `notebook/01_mlp.ipynb` | PyTorch walkthrough: single neuron → MLP → same net as BDT |
| `app/` | BDT re-expressed as a stop in the shared tour framework (train in browser preserved) |
| `curriculum/ep1_curriculum.md` | Narrative — neurons, layers, backprop intuition, training dynamics |

## What not to do

Do not touch `github.com/provandal/binarydigittrainer` during this port. That repo is the webinar artifact — let it be.
